"""
Factor entry widget.

Tickets: B1 (continuous rows), B2 (categorical rows), B4 (persistence).
Status: stub — implemented in Epic B.
"""

import streamlit as st


def render_factor_table() -> None:
    """Render the interactive factor entry table (Epic B)."""
    st.info("Factor entry UI — coming in Epic B (tickets B1–B4).")
