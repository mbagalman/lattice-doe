"""
Shared power parameter widgets (alpha, power, sigma, max_n).

Ticket: C5.
Status: stub — implemented in Epic C.
"""

import streamlit as st


def render_power_params() -> None:
    """Render shared power parameters (alpha, power target, sigma, max_n). Ticket C5."""
    st.info("Shared power parameters — coming in Epic C (ticket C5).")
